import 'package:coffe_app/model/profiles.dart';

class FoodSlider {
  int id;
  String title;
  String image;
  int raiting;
  List<Profile> profile;
  FoodSlider({this.id, this.title, this.image, this.raiting, this.profile});
}
